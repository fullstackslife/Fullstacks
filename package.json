{
  "name": "fullstacks-ink",
  "version": "1.0.0",
  "private": true,
  "description": "Static FullStacks.ink website served by a minimal Railway-compatible Node server.",
  "scripts": {
    "build": "node scripts/build.js",
    "migrate": "node scripts/migrate.js",
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "pg": "^8.21.0"
  }
}
